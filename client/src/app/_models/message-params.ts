export interface MessageParams {
  SenderEmail: string;// Who they want to see from. Can't be the same person as the user
  Username: string; // This her is the person using the app. The client
  Container: string;
}
